import { CreatePolicyView } from "@/features/policy/views/CreatePolicyView"

export const metadata = {
    title: "Nowa polityka — Policy Orchestrator",
}

export default function NewPolicyPage() {
    return <CreatePolicyView />
}
